{
	"Select Latest Block": {
		"prefix": "gLatestBlock",
		"body": [
			"-- Select data from latest block for table '${1:TableName}'",
			"SELECT * FROM ${1:TableName}",
			"WHERE upper(block_range) IS NULL"
		],
		"description": "Get latest data for table with block_range"
	}
}